---
sidebar_position: 4
---
# Date And TimePicker

import DateAndTimePicker from "../../src/components/DateAndTimePicker"
import { useState } from "react";
import MultipleDateAndTimePickerExample from "../components/multipickerexample"


The `DateAndTimePicker` component is a versatile, responsive UI element designed for effortless date and time selection. It provides an intuitive calendar interface with support for:
- **Date Range Control:** Limit user selection via `minDate` and `maxDate`.
- **Localization:** Format dates to various locales using the `locale` prop.
- **Custom Navigation:** Easily customize navigation controls using `forwardIcon` and `backwardIcon`.
- **Time Integration:** Optionally incorporate time selection through the `timeModule`.

Built with accessibility in mind, and leveraging DasiyUI and Tailwind CSS, this component ensures a modern, mobile-first experience across all devices.

### Light (default)
<div className="flex md:flex-row flex-col gap-4">
    <div data-theme="light" className="rounded-2xl w-48" style={{ width: "390px" }} >
        <div className="opacity-50 text-xs">Date and Time</div>
        <DateAndTimePicker value="2025-01-01 12:00" onChange={() => {}} locale="en" weekStart="sunday" dateProps/>
    </div>
    <div data-theme="light" className="rounded-2xl w-48" style={{ width: "390px" }} >
        <div className="opacity-50 text-xs">Combined</div>
        <DateAndTimePicker value="2025-01-01 12:00" combined onChange={() => {}} locale="en" weekStart="sunday" />
    </div>
</div>


## Multiple Date and Time Pickers 
`DateAndTimePicker` is primarly used for start and end date pickers.
Key features are
* makes the min date 

<MultipleDateAndTimePickerExample />


## Props

| Prop               | Type                                        | Description                                                                                                   |
| ------------------ | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `value`            | `string | null | undefined | Date`                             | The current selected date/time value. Can be a string, null, undefined, or a Date object.                     |
| `locale`           | `string`                                    | Specifies the locale for date formatting and calendar display (e.g., "en-US").                                |
| `minDate`          | `string | Date` (optional)                  | The minimum selectable date. Limits selection to dates on or after this value.                                |
| `maxDate`          | `string | Date` (optional)                  | The maximum selectable date. Limits selection to dates on or before this value.                               |
| `onChange`         | `(date: string) => void`                    | Callback invoked when the date/time value changes, passing the updated value as a string.                     |
| `placeholder`      | `string` (optional)                         | Placeholder text in the input field when no date is selected.                                                 |
| `className`        | `string` (optional)                         | Additional CSS classes for the outer container of the DatePicker.                                             |
| `combined`         | `boolean` (optional)                        | Determines if the date and time pickers are combined.                                                         |
| `AMPM`             | `boolean` (optional)                        | Determines if the time picker should use AM/PM format.                                                        |
| `dateProps`        | `DatePickerProps` (optional)                | Additional props for the date picker component. [See DatePickerProps](/datepicker)                                  |
| `timeProps`        | `TimePickerProps` (optional)                | Additional props for the time picker component. [See TimePickerProps](/timepicker)                                  |
